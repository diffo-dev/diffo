defmodule Diffo.Repo.Migrations.SpecificationIdentities do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    create(
      unique_index(:specifications, [:name, :major_version],
        name: "specifications_unique_major_version_per_name_index"
      )
    )

    create(
      unique_index(:specifications, [:name, :type],
        name: "specifications_unique_type_per_name_index"
      )
    )
  end

  def down do
    drop_if_exists(
      unique_index(:specifications, [:name, :type],
        name: "specifications_unique_type_per_name_index"
      )
    )

    drop_if_exists(
      unique_index(:specifications, [:name, :major_version],
        name: "specifications_unique_major_version_per_name_index"
      )
    )
  end
end
